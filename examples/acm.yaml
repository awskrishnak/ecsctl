apiVersion: ecs/v1
kind: Certificate
metadata:
  name: api.example.com
spec:
  domainName: api.example.com
  subjectAlternativeNames:
    - "*.api.example.com"
  validationMethod: DNS
  tags:
    - Key: Environment
      Value: production
